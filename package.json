{
    "scripts": {
        "lint": "eslint . --ext .js --no-error-on-unmatched-pattern",
        "test-unit": "mocha --recursive --exit"
    },
    "devDependencies": {
        "eslint": "^8.43.0",
        "jsonlint": "^1.6.3",
        "lodash": "^4.17.21",
        "mocha": "^10.2.0",
        "sinon": "^17.0.1"
    }
}
